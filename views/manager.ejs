<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manager</title>
    <link rel="stylesheet" href="/css/manager.css">
</head>
<body>
    <h1>Welcome to manager page...</h1>
    <% for(let i=0; i<users.length; i++) { %>
        <div class="user">
            <div class="avatar">
                <img src="<%= users[i].avatar %>" alt="<%= users[i].name %>">
            </div>
            <div class="name">
                <%= users[i].name %>
            </div>
        </div>
    <% } %>

    <div><a href="/signoutManager">Signout</a></div>
</body>
</html>